extends /app/pages/templates/language-landing  

block configParams
  -
    title        = "Create and Deploy Lisp Framework Applications | Nanobox"
    description  = "Learn how to setup a Lisp framework development environment and configure it to deploy to production."
    keywords     = "lisp framework, lisp config, lisp development environment, lisp production apps, lisp apps, deploy lisp, create lisp apps, config lisp apps"
    formalName   = "Lisp"
    informalName = "lisp"
    icon         = "lisp"
    frameworks   = "lisp"

block jsParams
  script.
    //- This is javascript
    articleGroups = ""
    devMode = true
