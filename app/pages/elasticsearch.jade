extends /app/pages/templates/framework-landing

block configParams
  -
    title        = "Deploy and Manage Elasticsearch in Production and Locally | Nanobox"
    description  = "How to create, configure, and manage a Elasticsearch database locally and in production with Nanobox."
    keywords     = "deploy elasticsearch, elasticsearch in production, elasticsearch, scaling elasticsearch database, local elasticsearch, elasticsearch local development, configure elasticsearch"
    formalName   = "Elasticsearch"
    informalName = "Elasticsearch"
    icon         = "elasticsearch"
    noQuickstart = true

block jsParams
  script.
    //- This is javascript
    articleGroups = "elasticsearch"
    requiredComponents = [
      {name:'Elasticsearch', icon:'elasticsearch', href:'#'}
    ]
