extends /app/pages/templates/article

block configParams
  -
    title       = "Create a Redis Component in Local and Production Environments | Nanobox"
    description = "Instructions for creating to a Redis component in development and production with Nanobox"
    keywords    = "deploy redis, redis in production, redis, scaling redis database, local redis, local development, configure redis"
    articles    = "redis"
    icons       = "redis"
    id          = "Create a Redis Component"

block markdown
  include:md /articles/redis/create.md
