extends /app/pages/templates/article  

block configParams
  -
    title       = "Securely Connect to Redis in Production and Development | Nanobox"
    description = "Instructions for connecting to Redis in development and production with Nanobox."
    keywords    = "deploy redis, redis in production, redis, scaling redis database, local redis, local development, configure redis"
    articles    = "redis"
    icons       = "redis"
    id          = "Connect to Redis"
    
block markdown
  include:md /articles/redis/connect.md
