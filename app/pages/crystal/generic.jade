extends /app/pages/templates/framework-landing  

block configParams
  -
    title        = "Create and Deploy a Production Crystal App | Nanobox"
    description  = "Learn how to setup a Crystal development environment and configure it to deploy to production."
    keywords     = "crystal, crystal config, crystal development environment, crystal production app, crystal app, deploy crystal, create crystal app, config crystal app"
    formalName   = "Crystal"
    informalName = "Crystal"
    icon         = "crystal"
    iconCrumbs   = "crystal"
    noQuickstart = true

block jsParams
  script.
    //- This is javascript
    requiredComponents = [
      {name:'Crystal', icon:'crystal', href:'#'}
    ]
    optionalComponents = [
      {name:'Postgres', icon:'postgres', href:'#'},
      {name:'MongoDB', icon:'mongo', href:'#'},
      {name:'Redis', icon:'redis', href:'#'}
    ]
    articleGroups = "crystal/generic"
