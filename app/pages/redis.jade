extends /app/pages/templates/framework-landing

block configParams
  -
    title        = "Deploy and Manage Redis in Production and Locally | Nanobox"
    description  = "How to create, configure, and manage a Redis database locally and in production with Nanobox."
    keywords     = "deploy redis, redis in production, redis, scaling redis, local redis, local development, configure redis"
    formalName   = "Redis"
    informalName = "Redis"
    icon         = "redis"
    noQuickstart = true

block jsParams
  script.
    //- This is javascript
    articleGroups = "redis"

    requiredComponents = [
      {name:'Redis', icon:'redis', href:'#'}
    ]
