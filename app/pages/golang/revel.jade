extends /app/pages/templates/framework-landing  

block configParams
  -
    title        = "Create and Deploy a Production Revel App | Nanobox"
    description  = "Learn how to setup a Revel development environment and configure it to deploy to production."
    keywords     = "revel, revel framework, golang frameworks, revel config, revel development environment, revel production app, revel app, deploy revel, create revel app, config revel app"
    formalName   = "Revel"
    informalName = "revel"
    icon         = "revel"
    iconCrumbs   = "golang,revel"

block jsParams
  script.
    //- This is javascript
    requiredComponents = [
      {name:'Golang', icon:'golang', href:'#'},
      {name:'Postgres', icon:'postgres', href:'#'}
    ]
    optionalComponents = [
      {name:'MongoDB', icon:'mongo', href:'#'},
      {name:'Redis', icon:'redis', href:'#'}
    ]
    articleGroups = "golang/revel"
    devMode = true
