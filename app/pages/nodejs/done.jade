extends /app/pages/templates/framework-landing  

block configParams
  -
    title        = "Create and Deploy a Production DoneJS App | Nanobox"
    description  = "Learn how to setup a DoneJS development environment and configure it to deploy to production."
    keywords     = "done, done framework, node js frameworks, done config, done development environment, done production app, done app, deploy done, create done app, config done app"
    formalName   = "DoneJS"
    informalName = "done"
    icon         = "done"
    iconCrumbs   = "node,done"
    launchTime   = "12"

block jsParams
  script.
    //- This is javascript
    requiredComponents = [
      {name:'Nodejs', icon:'node', href:'#'},
      {name:'Postgres', icon:'postgres', href:'#'}
    ]
    optionalComponents = [
      {name:'MongoDB', icon:'mongo', href:'#'},
      {name:'Redis', icon:'redis', href:'#'}
    ]
    articleGroups = "nodejs/done"
