run.config:
  # engine: nanobox-io/nanobox-engine-tolmark-gulp
  image: nanobox/build:beta
  engine: nanobox-io/nanobox-engine-tolmark-gulp#feature/workflow-refactor
  extra_packages:
    - fontconfig
    - freetype2
  # config:
  #   nodejs_runtime: 'nodejs-0.12'
